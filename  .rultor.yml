architect:
  - YaRiabtsev
assets:
  gitconfig: YaRiabtsev/private#assets/.gitconfig
install: |-
  sudo apt update
merge:
  script: |-
    echo ok
release:
  script: |-
    [[ "${tag}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || exit -1
